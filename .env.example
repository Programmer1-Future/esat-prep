# ESAT Supabase project (separate from tmuaprep). Copy to .env and fill in.
# Both values are from Project Settings → API. The anon key is publishable/safe
# for client code; RLS is what protects the data.
VITE_SUPABASE_URL=https://ctrqmaldgertvrabyipc.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here
